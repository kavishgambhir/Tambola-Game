import * as NumberGridActions from './number-grid.actions';

describe('NumberGrid', () => {
  it('should create an instance', () => {
    expect(new NumberGridActions.LoadNumberGrids()).toBeTruthy();
  });
});
